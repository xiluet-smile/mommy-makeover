{
  "name": "mommy-makeover-lp",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "wrangler pages dev site",
    "deploy": "wrangler pages deploy site"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
